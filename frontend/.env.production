# VITE_API_URL is NOT hardcoded here.
# It is injected automatically by the backend CI/CD pipeline as a GitHub Secret (VITE_API_URL)
# and passed to `npm run build` via the frontend-ci-cd.yml workflow.
# Do NOT commit a URL here — it will be outdated the next time the backend deploys.
